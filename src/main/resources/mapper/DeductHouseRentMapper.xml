<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zihao.taxhelperai.mapper.DeductHouseRentMapper">

    <select id="selectByDeductId" resultType="com.zihao.taxhelperai.model.entity.DeductHouseRent">
        SELECT 
            id,
            deduct_id AS deductId,
            rent_address AS rentAddress,
            city_level AS cityLevel,
            has_house_in_city AS hasHouseInCity,
            monthly_rent AS monthlyRent,
            monthly_amount AS monthlyAmount
        FROM deduct_house_rent
        WHERE deduct_id = #{deductId}
    </select>

    <delete id="deleteByDeductId">
        DELETE FROM deduct_house_rent
        WHERE deduct_id = #{deductId}
    </delete>

</mapper>