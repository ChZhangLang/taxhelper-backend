<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zihao.taxhelperai.mapper.DeductContinueEducationMapper">

    <select id="selectByDeductId" resultType="com.zihao.taxhelperai.model.entity.DeductContinueEducation">
        SELECT 
            id,
            deduct_id AS deductId,
            education_type AS educationType,
            education_name AS educationName,
            institution_name AS institutionName,
            certificate_no AS certificateNo,
            monthly_amount AS monthlyAmount
        FROM deduct_continue_education
        WHERE deduct_id = #{deductId}
    </select>

    <delete id="deleteByDeductId">
        DELETE FROM deduct_continue_education
        WHERE deduct_id = #{deductId}
    </delete>

</mapper>