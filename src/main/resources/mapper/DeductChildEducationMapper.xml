<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zihao.taxhelperai.mapper.DeductChildEducationMapper">

    <select id="selectByDeductId" resultType="com.zihao.taxhelperai.model.entity.DeductChildEducation">
        SELECT 
            id,
            deduct_id AS deductId,
            child_name AS childName,
            child_id_card AS childIdCard,
            education_stage AS educationStage,
            school_name AS schoolName,
            is_shared AS isShared,
            shared_ratio AS sharedRatio,
            monthly_amount AS monthlyAmount
        FROM deduct_child_education
        WHERE deduct_id = #{deductId}
    </select>

    <delete id="deleteByDeductId">
        DELETE FROM deduct_child_education
        WHERE deduct_id = #{deductId}
    </delete>

</mapper>