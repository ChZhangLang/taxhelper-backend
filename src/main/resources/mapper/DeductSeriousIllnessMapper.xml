<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zihao.taxhelperai.mapper.DeductSeriousIllnessMapper">

    <select id="selectByDeductId" resultType="com.zihao.taxhelperai.model.entity.DeductSeriousIllness">
        SELECT 
            id,
            deduct_id AS deductId,
            patient_name AS patientName,
            patient_relation AS patientRelation,
            total_medical_expense AS totalMedicalExpense,
            insurance_reimburse AS insuranceReimburse,
            self_pay_expense AS selfPayExpense,
            deductible_amount AS deductibleAmount,
            year
        FROM deduct_serious_illness
        WHERE deduct_id = #{deductId}
    </select>

    <delete id="deleteByDeductId">
        DELETE FROM deduct_serious_illness
        WHERE deduct_id = #{deductId}
    </delete>

</mapper>