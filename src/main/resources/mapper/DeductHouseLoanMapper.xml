<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zihao.taxhelperai.mapper.DeductHouseLoanMapper">

    <select id="selectByDeductId" resultType="com.zihao.taxhelperai.model.entity.DeductHouseLoan">
        SELECT 
            id,
            deduct_id AS deductId,
            house_address AS houseAddress,
            is_first_house AS isFirstHouse,
            loan_bank AS loanBank,
            loan_start_date AS loanStartDate,
            total_months AS totalMonths,
            used_months AS usedMonths,
            monthly_amount AS monthlyAmount
        FROM deduct_house_loan
        WHERE deduct_id = #{deductId}
    </select>

    <delete id="deleteByDeductId">
        DELETE FROM deduct_house_loan
        WHERE deduct_id = #{deductId}
    </delete>

</mapper>